version: 2
jobs:
  build:
    docker:
      - image: ubuntu:14.04
    steps:
      - checkout
      - run: |
          sudo apt-get update
          sudo apt-get upgrade


  test:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: echo "test"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build