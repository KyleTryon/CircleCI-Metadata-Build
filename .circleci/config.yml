version: 2.1
jobs:
  build:
    parameters:
      object:
        default: '{"hello": true, "greeting": "welcome"}'
        type: string
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          command: |
            TESTME="2"
            export $TESTME >> $BASH_ENV
            cat $BASH_ENV
      - run: echo $TESTME

