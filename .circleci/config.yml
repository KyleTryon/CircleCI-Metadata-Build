version: 2.1
# test
jobs:
  build:
    docker:
      - image: circleci/elixir:1.7.3-node-browsers
    steps:
      - checkout
      - setup_remote_docker
      - run:
          command: |
            docker-compose up