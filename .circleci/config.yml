version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:8.9.3

    steps:
      - run: sudo apt-get install locales
      - run: sudo update-locale LANG=fr_FR.UTF-8 LC_MESSAGES=fr_FR.UTF-8
      - run: echo $LANG
      - checkout
      - run: npm install
      - run: echo $LANG


