version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo test 2
      - run:
          command: |
            echo Running test
            set +e
            mkdir /tmp/test-results/x/y

      - run:
          shell: /bin/sh
          command: |
            echo Running test
            mkdir /tmp/test-results/b/c

